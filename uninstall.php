<?php
/**
 * Uninstall handler for No Crumbs.
 *
 * Выполняется при удалении плагина через админку WordPress.
 * Плагин не создаёт собственных записей в базе данных,
 * поэтому серверная очистка не требуется.
 *
 * Cookie согласия (nc_accepted) хранится на стороне клиента (браузера)
 * и автоматически истекает через 30 дней.
 *
 * @package No_Crumbs
 */

// Защита от прямого вызова
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}

// No data to clean up.
// Плагин не сохраняет данные в wp_options и не создаёт таблиц.
